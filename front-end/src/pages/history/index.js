import React, { Component } from "react";
import Layout from "../../common/Layout.js";

export default class index extends Component {
  render() {
    return (
      <Layout {...this.props}>
        <h5>this is history</h5>
      </Layout>
    );
  }
}
